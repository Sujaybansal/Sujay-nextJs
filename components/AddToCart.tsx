"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("click 1")}>Add to Cart </button>
    </div>
  );
};

export default AddToCart;
